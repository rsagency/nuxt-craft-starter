/**
 * Nuxt will use the configuration defined in the generate property when calling `nuxt.generate()`.
 */

export default {
    /**
     * Forces the error.vue layout to be displayed for
     * any pages on the site when deployed to Netlify.
     */
    fallback: true,
}
